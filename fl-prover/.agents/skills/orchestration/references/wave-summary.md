# Wave Summary

After each wave:

```bash
uv run python cli_tools/control.py wave --workspace WORKSPACE --wave N
```

The Regulator reviews the generated summary and returns task ledger patch recommendations.
